
## 2026-07-07 学習メモ

### Java if文

- `if` を使うと条件によって処理を分けられる
- `else` は条件に当てはまらなかった場合の処理
- `else if` を使うと複数条件を上から順番に判定できる
- 条件の順番が重要
- `>=` は以上、`==` は等しい、`!=` は等しくない


## 2026-07-08 学習メモ

### Java if文
